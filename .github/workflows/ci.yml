name: CI - Test & Validate

on:
  push:
    branches: [ main, develop, 'fix-verification-error*' ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  shellcheck:
    name: ShellCheck - Análisis Estático
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Ejecutar ShellCheck en scripts
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: './scripts'
          severity: warning
          check_together: 'yes'

  test-debian:
    name: Test en Debian 12 (Bookworm)
    runs-on: ubuntu-latest
    container:
      image: debian:12

    steps:
      - name: Instalar dependencias básicas
        run: |
          apt-get update
          apt-get install -y git curl ca-certificates

      - name: Checkout código
        uses: actions/checkout@v4

      - name: Test instalación (dry-run)
        run: |
          cd scripts
          bash setup_terminal.sh --dry-run --yes

      - name: Test instalación local
        run: |
          cd scripts
          bash setup_terminal.sh --local --yes

      - name: Verificar instalación
        run: |
          cd scripts
          bash verify.sh

  test-macos:
    name: Test en macOS (Latest)
    runs-on: macos-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Test instalación (dry-run)
        run: |
          cd scripts
          bash setup_terminal.sh --dry-run --yes

      - name: Test instalación en macOS
        run: |
          cd scripts
          bash setup_terminal.sh --yes

      - name: Verificar instalación
        run: |
          cd scripts
          bash verify.sh --verbose

      - name: Test que neofetch funciona
        run: neofetch --stdout

      - name: Test que starship funciona
        run: starship --version

      - name: Test desinstalación
        run: |
          cd scripts
          bash uninstall.sh --yes

  validate-configs:
    name: Validar Configuraciones
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Instalar Starship para validación
        run: |
          curl -sS https://starship.rs/install.sh | sh -s -- --yes

      - name: Validar configuración de Starship
        run: |
          export STARSHIP_CONFIG=./config/starship.toml
          starship print-config > /dev/null && echo "✅ Configuración de Starship válida"

      - name: Verificar sintaxis de Neofetch config
        run: |
          if [ -f config/neofetch.conf ]; then
            bash -n config/neofetch.conf && echo "✅ Sintaxis de Neofetch válida"
          fi

  security-scan:
    name: Escaneo de Seguridad
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Buscar secretos con Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

  documentation:
    name: Validar Documentación
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Verificar README existe
        run: |
          test -f README.md && echo "✅ README encontrado"

      - name: Verificar CONTRIBUTING existe
        run: |
          test -f CONTRIBUTING.md && echo "✅ CONTRIBUTING encontrado" || echo "⚠️ CONTRIBUTING no encontrado"

      - name: Verificar LICENSE existe
        run: |
          test -f LICENSE && echo "✅ LICENSE encontrado" || echo "⚠️ LICENSE no encontrado"

      # Markdownlint deshabilitado - genera demasiado ruido en CI/CD
      # - name: Lint Markdown
      #   uses: actionshub/markdownlint@main
      #   with:
      #     path: .
      #     filesToIgnoreRegex: "node_modules"

  summary:
    name: Resumen de CI
    runs-on: ubuntu-latest
    needs: [shellcheck, test-debian, test-macos, validate-configs]
    if: always()

    steps:
      - name: Verificar resultados
        run: |
          echo "🎉 Todos los tests de CI completados"
          echo "Estado: ${{ job.status }}"
